services:
  - type: web
    name: pixel-horde
    runtime: static
    buildCommand: "echo no build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
